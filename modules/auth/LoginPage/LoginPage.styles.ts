import { createStyles } from '@mantine/core';

export const loginPageStyles = createStyles(() => ({
  container: {
    position: 'relative',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '100vh',
  },
}));
